import React from 'react';

function ball(props) {
  return (
 //ball radius = ball component
 var ballRadius = 10;

  // dx and dy = ball component
  var dx = 2;
  var dy = -2;
    )
  }
  
  export default ball
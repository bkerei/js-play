import React from 'react';

function paddle(props) {
  return (
    // paddleheight, paddleWidth and paddleX = paddle component
    var paddleHeight = 10;
    var paddleWidth = 75;
    var paddleX = (canvas.width - paddleWidth) / 2;

    )
  }
  
  export default paddle